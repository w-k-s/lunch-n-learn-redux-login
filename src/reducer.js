const INITIAL_STATE = {
	form:{},
	responseData:null,
	responseError:null,
	submitting: false
}

export const reducer =  (state = INITIAL_STATE, action)=>{
	switch(action.type){
		default:
			return state;
	}
}